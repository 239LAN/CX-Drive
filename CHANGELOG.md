# 更新日志

版本号以根目录 [VERSION](VERSION) 为准：页脚展示与自动更新的版本比对均依赖它。
发布新版本时先改 `VERSION`，再执行 `python build_install.py`，把生成的
`dist/RealFiles-release-<版本>-install.sh` 作为 GitHub Release 附件上传（自动更新只识别该命名）。

## 1.3.3

### 新增

- 远端存储点（FTP / SFTP / S3）的读取与写入统一改为后台任务：上传请求立即返回，
  下载 / 预览 / 解压 / 压缩在后台取回并跳转进度页轮询，不再因等待远端传输而超时
- 文件列表对传输中的文件标记「取回中 / 上传中」并自动刷新进度，完成后刷新列表
- 存储点健康检查：读写失败自动摘除、暂停写入新文件，管理后台「存储点」页显示
  「连接异常」及原因与检测时间，每小时巡检自动恢复

### 变更

- 正在上传到远端（尚未完成）的文件可直接预览 / 下载（读取本地暂存文件）
- 秒传校验只认本地已有实体，不再为校验把远端文件整份拉回本地
- 缓存健壮性：命中缓存自动续期、超过容量上限按 LRU 淘汰、正在传输的文件不会被 TTL 清理
- 数据库新增 `transfer_tasks` 表；`storage_points` 表补齐健康检查字段

## 1.3.2

### 新增

- 检测到新版本时，管理员点击页脚的版本提示即弹出模态框展示本页更新日志，
  并可直接点「立即更新」在后台完成下载与覆盖安装，无需再手动下载安装脚本执行
  （非管理员仍为跳转 Release 页面查看）

## 1.3.1

### 新增

- 存储点新增 SFTP 与 S3（对象存储）协议支持：SFTP 使用主机 / 端口 / 用户名 / 密码 / 远端目录；
  S3 支持自定义 Endpoint（兼容 MinIO、OSS 等）、Region、Bucket、AccessKey / SecretKey、
  前缀目录与「路径寻址（Path Style）」开关，远端文件读取同样先拉取到本地缓存再响应
- 「获取直链」功能（仅 SVIP）：在文件列表一键生成可公开访问的下载直链，也可在
  「我的分享」页统一管理，支持设置有效期（留空为永久）、随时撤销
- 直链限额：单条直链每周最多下载 5GB（按 ISO 自然周重置，超限后该周不可再下载）；
  每个 SVIP 用户每周最多生成 10 条直链（撤销不返还额度）

### 变更

- 未登录访客的默认下载限速由 1MB/s 调整为 10MB/s
- 安装脚本模板（原 `install-template.sh`）改为内嵌在 `build_install.py` 中，
  仓库不再保留独立模板文件；安装方式仅保留单文件安装包一种
- 数据库新增 `direct_links` / `direct_link_quotas` 表；`storage_points` 表补齐 S3 相关字段
- 新增依赖 `paramiko`（SFTP）与 `boto3`（S3）

## 1.3.0

### 新增

- 管理后台分栏为「用户管理 / 套餐管理 / 兑换码 / 存储点 / 设置」五个独立子页面，顶部标签栏切换
- 兑换码系统：批量生成随机兑换码（前缀、长度、数量、有效期、备注），支持
  会员 / 余额 / 叠加包三种类型；会员类可按秒级配置发放时长；兑换码一次性使用，
  兑换记录可查询、可清空，未使用的兑换码可按批次一键清空
- 套餐管理页整合套餐与叠加包：叠加包时长改为秒级配置（可选单位，自动换算展示）
- 设置页支持可视化编辑全部站点配置（站点信息、功能开关、自动更新、HTTPS），
  保存后写入 `config.yml`，并提供「重启服务」按钮在后台直接重启
- 会员支持一次性购买多月（按自然月精确顺延），前端会员中心可直接输入购买月数
- 自动更新可配置：管理员登录时检查更新、每日自动检查时间（默认 0 点）、
  是否自动安装更新，均可开关与调整；设置页可随时手动检查或立即更新
- 存储点管理：支持配置多个存储位置（本地磁盘多挂载点 / FTP），每个存储点必须填写
  容量上限，占用率达到 90% 即视为已满并停止写入；管理后台「存储点」页可查看
  各点占用进度、实时测连通性、启用 / 停用（停用后可读不可写）
- 均衡分配：新文件按各存储点当前占用率由低到高挑选落点，批量写入会预留已分配量，
  避免一次性写入全部堆到同一个点
- 删除存储点支持两种方式：数据转移到其他存储点（按 90% 计总容量之和判断空间是否足够，
  逐个均衡摊到各点）或在无法转移时直接删除数据（对应文件记录标记为「已丢失」，
  保留 7 天后自动清理，期间文件列表带标记并在页面顶部横幅提示）
- FTP 存储点上的文件在下载 / 预览时自动拉取到本地缓存目录再响应，缓存按 TTL 定时清理

### 变更

- 管理员赠送会员支持秒级时长（可选秒 / 分 / 时 / 天 / 月单位）
- 会员记录区分来源（购买 / 管理员赠送 / 兑换码）
- 数据库新增 `storage_points` 表，历史文件与远程下载记录在升级时自动归属到默认存储点
- 安装脚本会创建并保留 `storage_cache` 目录（FTP 缓存），覆盖更新时不删除
- 项目全面更名为 RealFiles（不再使用中文名）：站点与项目名、法律与许可页面文案、
  对外 User-Agent 等标识统一改为英文
- Linux 侧标识统一为 `realfiles`：安装目录 `/opt/realfiles`、运行用户 `realfiles`、
  服务 `realfiles.service`、更新与重启入口 `/usr/local/sbin/realfiles-auto-update`、
  `realfiles-restart`、sudoers 规则 `/etc/sudoers.d/realfiles-update`
- 环境变量前缀由 `CLOUDPAN_` 改为 `REALFILES_`（应用仍兼容读取旧前缀）；
  SQLite 默认库文件由 `cloud_drive.db` 改名为 `realfiles.db`
- 发布产物改名为 `RealFiles-release-<版本>-install.sh`，单文件安装包内嵌负载标记改为
  `REALFILES_BEGIN` / `REALFILES_END`；自动更新同时识别旧的 `CXDrive-release-*.sh`，
  旧版本实例可平滑升级到首个 RealFiles 版本
- 安装脚本支持自动迁移旧安装：检测到 `/opt/cx-pan` 时停用并移除旧服务与旧入口，
  把数据库、用户数据、`.env`、`config.yml` 迁移到 `/opt/realfiles`

## 1.2.1

### 新增

- 上传安全校验改为扩展名白名单：仅放行文档、图片、音视频、压缩包等常见格式，
  配合文件头嗅探拦截伪装成常见格式的可执行程序与脚本；策略覆盖上传 / 秒传 /
  重命名 / 远程下载 / 解压五个入口
- 解压时压缩包内不在白名单的条目会被跳过，并在提示中列出被跳过的文件，其余文件照常解压

### 变更

- 确认与提示弹窗改用 Bootstrap 模态框，替换浏览器原生的 `confirm` / `alert`
  （危险操作用红色确认按钮，删除类表单一律先弹窗确认）
- 构建产物统一为 `dist/CXDrive-release-<版本>.sh`，不再额外输出 `install.sh`
- 单文件安装包的内嵌负载标记更名为 `CX_DRIVE_BEGIN` / `CX_DRIVE_END`
- 清理仓库根目录遗留的旧版安装脚本

### 修复

- 上传被拒绝时前端不再静默失败，会直接显示服务端返回的拒绝原因（例如扩展名不在白名单）

## 1.2.0

### 新增

- 在线压缩与解压：选中文件 / 文件夹一键打包为 zip 存入网盘，zip 包可在网页上直接解压
- 分享转存：访客通过分享链接浏览时可一键把文件转存到自己的网盘
- 自动更新：每天 0 点请求 GitHub Releases 比对版本，发现新版本时下载发布脚本
  （校验 sha256 后以 root 覆盖更新），可在 `config.yml` 的 `update` 段关闭；
  页脚展示当前版本号与「有新版本」提示
- GitHub 代理回退：直连 `github.com` / `api.github.com` 失败时改用公共代理，便于国内服务器更新
- 未登录访问分享页时的横幅提示

### 变更

- 「保持登录状态」（原「记住我」）有效期由会话级改为 7 天
- 已登录用户默认不限速

### 修复

- 网盘内下载不再丢失响应中的文件大小与扩展名
